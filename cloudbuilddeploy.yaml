steps:

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'bash'
    args:
    - '-c'
    - > 
      gcloud deploy releases create release-$BUILD_ID
      --delivery-pipeline=demo-la-cabra
      --region=us-central1
      --images=my-app-image=us-east1-docker.pkg.dev/$PROJECT_ID/repo-sub/auth
      --source=./

options:
  logging: CLOUD_LOGGING_ONLY